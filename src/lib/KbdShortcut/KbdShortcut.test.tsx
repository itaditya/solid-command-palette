import { describe, test, expect } from 'vitest';

describe('Test KbdShortcut', () => {
  test('should render Control key as modifier on Windows', () => {
    expect(4 + 5, 'Adding 4 with 5 equals 9').toBe(9);
  });
});
